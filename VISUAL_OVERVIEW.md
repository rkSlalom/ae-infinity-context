# AE Infinity - Visual Overview

**Working Directory Pattern for Agentic Development**

---

## 🎯 The Big Picture

```
┌─────────────────────────────────────────────────────────────────┐
│                     AGENTIC DEVELOPMENT WORKFLOW                │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Human/AI   │
│    Agent     │
└──────┬───────┘
       │
       │ 1. Run setup script
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ./scripts/setup-working-directory.sh                                    │
│  ────────────────────────────                                    │
│  ✓ Check prerequisites (Git, .NET, Node.js)                     │
│  ✓ Clone all 3 repositories                                     │
│  ✓ Install dependencies                                         │
│  ✓ Build & verify                                               │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ Creates isolated workspace
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  work/                                                           │
│  ├── ae-infinity-api/         ✅ Built & ready                  │
│  ├── ae-infinity-ui/          ✅ Dependencies installed          │
│  └── ae-infinity-context/     ✅ Specs ready                     │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 2. Agent reads context
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  work/ae-infinity-context/                                       │
│  ├── PROJECT_SPEC.md          → Project requirements            │
│  ├── API_SPEC.md              → API contracts                   │
│  ├── ARCHITECTURE.md          → System design                   │
│  ├── personas/                → User personas                   │
│  ├── journeys/                → User workflows                  │
│  └── features/                → Feature specs                   │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 3. Agent implements feature
       ▼
┌─────────────────────────────┐    ┌─────────────────────────────┐
│  work/ae-infinity-api/      │    │  work/ae-infinity-ui/       │
│  ────────────────────        │    │  ────────────────────        │
│  Modify:                    │    │  Modify:                    │
│  • Controllers              │    │  • Components               │
│  • Services                 │    │  • Pages                    │
│  • Entities                 │    │  • Services                 │
│  • Handlers                 │    │  • Hooks                    │
└─────────────┬───────────────┘    └─────────────┬───────────────┘
              │                                    │
              │ 4. Build & test                   │
              ▼                                    ▼
       ┌──────────────┐                    ┌──────────────┐
       │ dotnet build │                    │  npm build   │
       │  dotnet run  │                    │  npm run dev │
       └──────┬───────┘                    └──────┬───────┘
              │                                    │
              └────────────┬───────────────────────┘
                           │
                           │ 5. Human reviews
                           ▼
                    ┌──────────────┐
                    │  Review Code │
                    │  Run Tests   │
                    │  Verify API  │
                    └──────┬───────┘
                           │
                    ┌──────┴───────┐
                    │              │
                    ▼              ▼
              ┌──────────┐   ┌──────────┐
              │   GOOD   │   │   BAD    │
              └────┬─────┘   └────┬─────┘
                   │              │
                   │              │ 6. Decision
                   ▼              ▼
         ┌──────────────┐   ┌─────────────┐
         │ Merge to     │   │ rm -rf work/│
         │ main repos   │   │ & retry     │
         └──────────────┘   └─────────────┘
```

---

## 📊 Repository Structure

```
┌──────────────────────────────────────────────────────────────────────┐
│                      THREE-REPOSITORY ARCHITECTURE                   │
└──────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│  ae-infinity-context/                                                 │
│  ────────────────────                                                 │
│  📚 SINGLE SOURCE OF TRUTH                                            │
│                                                                       │
│  https://github.com/rkSlalom/ae-infinity-context.git                 │
│                                                                       │
│  Type: Documentation & Specifications                                │
│  Language: Markdown, JSON Schema                                     │
│  Build: None needed                                                  │
│                                                                       │
│  Contains:                                                            │
│  • Complete project requirements                                     │
│  • API contracts (REST + SignalR)                                    │
│  • System architecture                                               │
│  • UI component specs                                                │
│  • User personas & journeys                                          │
│  • JSON schemas for all API types                                    │
│  • Feature-driven documentation                                      │
└───────────────────────────────────────────────────────────────────────┘
                              │
                              │ Referenced by
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
│ ae-infinity-api/  │ │ ae-infinity-ui/   │ │  Developers/      │
│ ─────────────────  │ │ ─────────────────  │ │  AI Agents        │
│                   │ │                   │ │                   │
│ 🔧 BACKEND        │ │ 🎨 FRONTEND       │ │ 🤖 CONSUMERS      │
│                   │ │                   │ │                   │
│ github.com/       │ │ github.com/       │ │                   │
│ rkSlalom/         │ │ dallen4/          │ │                   │
│ ae-infinity-api   │ │ ae-infinity-ui    │ │                   │
│                   │ │                   │ │                   │
│ .NET 9.0 Web API  │ │ React 19 + TS     │ │ Read specs →      │
│ Clean Arch        │ │ Vite 7            │ │ Implement code    │
│ SQLite + EF Core  │ │ Tailwind CSS 3    │ │                   │
│ CQRS + MediatR    │ │ React Router 7    │ │                   │
│                   │ │                   │ │                   │
│ Build:            │ │ Build:            │ │                   │
│ dotnet restore    │ │ npm install       │ │                   │
│ dotnet build      │ │ npm run build     │ │                   │
│                   │ │                   │ │                   │
│ Run:              │ │ Run:              │ │                   │
│ dotnet run        │ │ npm run dev       │ │                   │
│ Port: 5233        │ │ Port: 5173        │ │                   │
└───────────────────┘ └───────────────────┘ └───────────────────┘
```

---

## 🔄 Data Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                         REQUEST/RESPONSE FLOW                        │
└──────────────────────────────────────────────────────────────────────┘

  User Browser
      │
      │ HTTP Request
      ▼
┌─────────────────────────────────┐
│  React UI (Port 5173)           │
│  ───────────────────             │
│  • Pages render                 │
│  • User interacts               │
│  • Service layer called         │
└────────────┬────────────────────┘
             │
             │ HTTP + JWT Token
             │ POST /api/lists
             ▼
┌─────────────────────────────────┐
│  .NET API (Port 5233)           │
│  ───────────────────             │
│  • Controller receives          │
│  • MediatR dispatches           │
│  • Handler processes            │
│  • Repository queries DB        │
└────────────┬────────────────────┘
             │
             │ EF Core Query
             ▼
┌─────────────────────────────────┐
│  SQLite In-Memory DB            │
│  ───────────────────             │
│  • Tables queried               │
│  • Data returned                │
└────────────┬────────────────────┘
             │
             │ Entity → DTO
             ▼
┌─────────────────────────────────┐
│  AutoMapper + Result Pattern    │
│  ───────────────────             │
│  • Entity mapped to DTO         │
│  • Wrapped in Result<T>         │
└────────────┬────────────────────┘
             │
             │ JSON Response
             ▼
┌─────────────────────────────────┐
│  React UI Updates               │
│  ───────────────────             │
│  • State updated                │
│  • UI re-renders                │
│  • User sees result             │
└─────────────────────────────────┘
```

---

## 🏗️ Architecture Layers

```
┌──────────────────────────────────────────────────────────────────────┐
│                      CLEAN ARCHITECTURE (API)                        │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Layer 4: API (Presentation)                                        │
│  ────────────────────────────                                        │
│  Controllers → Endpoints → HTTP                                     │
│  Dependencies: Infrastructure, Application                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 3: Infrastructure                                            │
│  ────────────────────────                                            │
│  DbContext, Repositories, Services, JWT, BCrypt                     │
│  Dependencies: Application, Domain                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 2: Application                                               │
│  ────────────────────                                                │
│  CQRS Handlers, DTOs, Validators, Interfaces                        │
│  Dependencies: Domain only                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 1: Domain (Core)                                             │
│  ───────────────────                                                 │
│  Entities, Value Objects, Exceptions                                │
│  Dependencies: NONE (pure business logic)                           │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                         AUTHENTICATION FLOW                          │
└──────────────────────────────────────────────────────────────────────┘

User                    UI                      API                   DB
 │                      │                       │                     │
 │ 1. Enter email/pwd   │                       │                     │
 ├─────────────────────>│                       │                     │
 │                      │                       │                     │
 │                      │ 2. POST /auth/login   │                     │
 │                      ├──────────────────────>│                     │
 │                      │    { email, pwd }     │                     │
 │                      │                       │                     │
 │                      │                       │ 3. Query user       │
 │                      │                       ├────────────────────>│
 │                      │                       │                     │
 │                      │                       │ 4. User + hashed pwd│
 │                      │                       │<────────────────────┤
 │                      │                       │                     │
 │                      │                       │ 5. BCrypt verify    │
 │                      │                       │ (password matches?) │
 │                      │                       │                     │
 │                      │                       │ 6. Generate JWT     │
 │                      │                       │ (1 hour expiry)     │
 │                      │                       │                     │
 │                      │ 7. Return token+user  │                     │
 │                      │<──────────────────────┤                     │
 │                      │                       │                     │
 │ 8. Store token       │                       │                     │
 │<─────────────────────┤                       │                     │
 │  (localStorage)      │                       │                     │
 │                      │                       │                     │
 │ 9. Redirect to dashboard                     │                     │
 │                      │                       │                     │
 │ ... Later requests ...                       │                     │
 │                      │                       │                     │
 │                      │ 10. GET /lists        │                     │
 │                      ├──────────────────────>│                     │
 │                      │ Authorization: Bearer │                     │
 │                      │      {JWT token}      │                     │
 │                      │                       │                     │
 │                      │                       │ 11. Validate JWT    │
 │                      │                       │ (signature, expiry) │
 │                      │                       │                     │
 │                      │                       │ 12. Query lists     │
 │                      │                       ├────────────────────>│
 │                      │                       │                     │
 │                      │                       │ 13. Lists data      │
 │                      │                       │<────────────────────┤
 │                      │                       │                     │
 │                      │ 14. Return lists      │                     │
 │                      │<──────────────────────┤                     │
 │                      │                       │                     │
 │ 15. Display lists    │                       │                     │
 │<─────────────────────┤                       │                     │
```

---

## 📦 Setup Script Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                    SETUP SCRIPT EXECUTION FLOW                       │
└──────────────────────────────────────────────────────────────────────┘

  ./scripts/setup-working-directory.sh [directory]
      │
      ▼
┌─────────────────────────────────┐
│ 1. Check Prerequisites          │
│    ─────────────────             │
│    ✓ Git installed?             │
│    ✓ .NET 9.0 SDK?              │
│    ✓ Node.js 20+?               │
│    ✓ npm 10+?                   │
│                                 │
│    If missing → Show install    │
│    links and exit               │
└────────────┬────────────────────┘
             │ ✓ All present
             ▼
┌─────────────────────────────────┐
│ 2. Setup Directory              │
│    ────────────────              │
│    • Check if exists            │
│    • Prompt to remove (if yes)  │
│    • Create directory           │
│    • Validate path              │
└────────────┬────────────────────┘
             │ ✓ Directory ready
             ▼
┌─────────────────────────────────┐
│ 3. Clone Repositories           │
│    ───────────────               │
│    ① Clone ae-infinity-api      │
│    ② Clone ae-infinity-ui       │
│    ③ Clone ae-infinity-context  │
│                                 │
│    Report: branch & commit info │
└────────────┬────────────────────┘
             │ ✓ All cloned
             ▼
┌─────────────────────────────────┐
│ 4. Setup API (.NET)             │
│    ────────────────              │
│    $ cd work/ae-infinity-api    │
│    $ dotnet restore             │
│    $ dotnet build               │
│                                 │
│    ✓ NuGet packages restored    │
│    ✓ Solution built             │
└────────────┬────────────────────┘
             │ ✓ API ready
             ▼
┌─────────────────────────────────┐
│ 5. Setup UI (React)             │
│    ────────────────              │
│    $ cd work/ae-infinity-ui     │
│    $ npm install                │
│    $ npm run build              │
│                                 │
│    ✓ Dependencies installed     │
│    ✓ Build verified             │
└────────────┬────────────────────┘
             │ ✓ UI ready
             ▼
┌─────────────────────────────────┐
│ 6. Setup Context (Docs)         │
│    ─────────────────             │
│    $ cd work/ae-infinity-context│
│    • Verify key documents       │
│    • No build needed            │
│                                 │
│    ✓ Documentation ready        │
└────────────┬────────────────────┘
             │ ✓ Context ready
             ▼
┌─────────────────────────────────┐
│ 7. Generate Summary             │
│    ─────────────────             │
│    • Show directory structure   │
│    • Quick start commands       │
│    • Test credentials           │
│    • Documentation links        │
│    • Next steps                 │
└────────────┬────────────────────┘
             │
             ▼
     ✅ SETUP COMPLETE!
     Ready for development
```

---

## 🎯 Use Case: AI Agent Workflow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      AI AGENT DEVELOPMENT CYCLE                      │
└──────────────────────────────────────────────────────────────────────┘

Step 1: Setup
─────────────
    $ ./scripts/setup-working-directory.sh agent-workspace
    ✓ Creates: agent-workspace/ with all 3 repos

Step 2: Load Context
────────────────────
    Agent reads:
    • agent-workspace/ae-infinity-context/PROJECT_SPEC.md
    • agent-workspace/ae-infinity-context/API_SPEC.md
    • agent-workspace/ae-infinity-context/features/[target]/
    
    Agent understands:
    • What needs to be built
    • How it should work
    • What patterns to follow

Step 3: Implement
─────────────────
    Agent modifies:
    • agent-workspace/ae-infinity-api/src/...
      OR
    • agent-workspace/ae-infinity-ui/src/...
    
    Agent follows:
    • Clean Architecture patterns
    • CQRS for backend
    • React patterns for frontend
    • Existing code style

Step 4: Build & Test
────────────────────
    Backend:
    $ cd agent-workspace/ae-infinity-api
    $ dotnet build
    $ dotnet run
    ✓ Compiles without errors
    ✓ API responds to requests
    
    Frontend:
    $ cd agent-workspace/ae-infinity-ui
    $ npm run build
    $ npm run dev
    ✓ TypeScript compiles
    ✓ UI renders correctly

Step 5: Human Review
────────────────────
    Human checks:
    • Code quality
    • Follows specifications
    • Tests pass
    • No regressions
    
    Decision:
    ┌──────────────┐
    │   APPROVED   │
    └──────┬───────┘
           │
    ┌──────┴───────┐
    │   CHANGES    │
    │   NEEDED     │
    └──────┬───────┘
           │
           ▼
    Go back to Step 3

Step 6: Integrate
─────────────────
    If approved:
    • Review git diff
    • Cherry-pick commits
    • Merge to main repos
    • Deploy

Step 7: Cleanup
───────────────
    $ rm -rf agent-workspace/
    
    Agent can start fresh next time
```

---

## 📈 Benefits Visualization

```
┌──────────────────────────────────────────────────────────────────────┐
│                     BEFORE vs AFTER COMPARISON                       │
└──────────────────────────────────────────────────────────────────────┘

BEFORE (Manual Setup)
─────────────────────
  Time: 15-20 minutes
  Steps: 15+ manual steps
  Errors: Common (missed steps, wrong versions)
  
  ┌───────────────────────────────────────┐
  │ 1. Clone API repo                     │
  │ 2. cd to API directory                │
  │ 3. Check .NET version                 │
  │ 4. dotnet restore                     │
  │ 5. Fix NuGet errors                   │
  │ 6. dotnet build                       │
  │ 7. Fix build errors                   │
  │ 8. Clone UI repo                      │
  │ 9. cd to UI directory                 │
  │ 10. Check Node version                │
  │ 11. npm install                       │
  │ 12. Fix dependency conflicts          │
  │ 13. Clone context repo                │
  │ 14. Try to remember what's next       │
  │ 15. Finally ready... maybe            │
  └───────────────────────────────────────┘
  
  Result: Frustration 😤

AFTER (Automated Setup)
───────────────────────
  Time: 2-3 minutes
  Steps: 1 command
  Errors: Prevented (automated checks)
  
  ┌───────────────────────────────────────┐
  │ $ ./scripts/setup-working-directory.sh        │
  │                                       │
  │ ... wait 2-3 minutes ...              │
  │                                       │
  │ ✓ All done!                           │
  └───────────────────────────────────────┘
  
  Result: Productivity 🚀

┌──────────────────────────────────────────────────────────────────────┐
│ TIME SAVED: 85% reduction                                            │
│ ERROR RATE: Near zero                                                │
│ DEVELOPER HAPPINESS: 📈 Significantly increased                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Technology Stack Overview

```
┌──────────────────────────────────────────────────────────────────────┐
│                          TECHNOLOGY STACK                            │
└──────────────────────────────────────────────────────────────────────┘

Backend (ae-infinity-api)
─────────────────────────
┌─────────────────────────────────────┐
│ .NET 9.0 SDK                        │
│ ├─ ASP.NET Core Web API             │
│ ├─ Entity Framework Core 9.0        │
│ │  └─ SQLite (in-memory)            │
│ ├─ MediatR 13.1.0 (CQRS)            │
│ ├─ FluentValidation 11.9.0          │
│ ├─ AutoMapper 13.0.1                │
│ ├─ BCrypt.Net-Next 4.0.3            │
│ ├─ Serilog 4.0.0 (logging)          │
│ └─ Swashbuckle 7.1.0 (Swagger)      │
└─────────────────────────────────────┘

Frontend (ae-infinity-ui)
─────────────────────────
┌─────────────────────────────────────┐
│ Node.js 20+ / npm 10+               │
│ ├─ React 19.1.1                     │
│ ├─ React DOM 19.1.1                 │
│ ├─ React Router 7.9.5               │
│ ├─ TypeScript 5.9.3                 │
│ ├─ Vite 7.1.7 (bundler)             │
│ ├─ Tailwind CSS 3.4.0               │
│ ├─ PostCSS 8.5.6                    │
│ └─ ESLint 9.36.0                    │
└─────────────────────────────────────┘

Documentation (ae-infinity-context)
───────────────────────────────────
┌─────────────────────────────────────┐
│ Markdown                            │
│ JSON Schema (Draft 7)               │
│ Bash scripts                        │
│ └─ No dependencies!                 │
└─────────────────────────────────────┘
```

---

## 🎓 Quick Reference

```
┌──────────────────────────────────────────────────────────────────────┐
│                         QUICK COMMAND REFERENCE                      │
└──────────────────────────────────────────────────────────────────────┘

Setup
─────
$ ./scripts/setup-working-directory.sh                  # Creates work/
$ ./scripts/setup-working-directory.sh my-dir           # Creates my-dir/
$ ./scripts/setup-working-directory.sh --help           # Show help

Run API
───────
$ cd work/ae-infinity-api/src/AeInfinity.Api
$ dotnet run
→ http://localhost:5233 (Swagger: /)

Run UI
──────
$ cd work/ae-infinity-ui
$ npm run dev
→ http://localhost:5173

Test Login
──────────
Email: sarah@example.com
Password: Password123!

View Docs
─────────
$ cd work/ae-infinity-context
$ cat PROJECT_SPEC.md
$ cat API_SPEC.md
$ ls features/

Cleanup
───────
$ rm -rf work/
```

---

**Visual overview complete!**  
**See full documentation in WORKING_DIRECTORY_GUIDE.md** 📚

